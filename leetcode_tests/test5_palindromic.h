//#include "stdafx.h"

#include <string>

using namespace std;
using std::string;

class SolutionTest5Palindromic {
public:
    string longestPalindrome(string s);
	string expandAroundCenter(string s, int c1, int c2);
};